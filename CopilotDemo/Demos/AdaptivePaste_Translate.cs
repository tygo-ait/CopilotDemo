namespace CopilotDemo.Demos
{
    internal class AdaptivePaste_Translate
    {
        // Demonstrates adaptive paste with translation
        // This feature allows you to paste code in a different language and have it translated to the current language

        // Original code in Python
        //def two_sum(nums, target):
        //     num_map = {}
        //     for i, num in enumerate(nums) :
        //         complement = target - num
        //         if complement in num_map:
        //             return [num_map[complement], i]
        //         num_map[num] = i
        //     return []

        // Paste uncommented code here:

    }
}
